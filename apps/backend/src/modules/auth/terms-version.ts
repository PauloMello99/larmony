/**
 * Versão vigente dos Termos de Uso + Política de Privacidade (LGPD).
 * Gravada em `users.terms_version` no aceite do cadastro. Ao publicar uma
 * revisão relevante dos documentos (frontend `pages/legal/*`), atualize esta
 * constante — contas novas passam a registrar a nova versão; revisões
 * relevantes disparam o modal de re-aceite bloqueante no frontend (ver
 * `TermsReacceptDialog`). Par acoplado no frontend:
 * `apps/frontend/src/shared/lib/legal-info.ts` (LEGAL_VERSION).
 */
export const TERMS_VERSION = "2026-07-27";
